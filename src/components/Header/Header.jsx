import React from 'react';
import './header.css';

const Header = (props) => {
    return (
        <header className='header'>
            Я хедер
        </header>
    );
};

export default Header;
